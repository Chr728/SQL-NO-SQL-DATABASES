SELECT t.author_id, COUNT(*)
FROM "Tweet" t
WHERE t.tweet_date BETWEEN (TIMESTAMP '2021-01-01 00:00:00' AT TIME ZONE 'EST') AND (TIMESTAMP '2022-01-01 00:00:00' AT TIME ZONE 'EST')
GROUP BY t.author_id
HAVING COUNT(*) > 1